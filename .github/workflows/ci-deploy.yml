name: CI and Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: whuang1101/odinbook-server

jobs:
  build-test-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - uses: docker/login-action@v3
        if: github.event_name == 'push'
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        if: github.event_name == 'push'
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

  deploy:
    needs: build-test-publish
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    env:
      LEARNING_VM_HOST: ${{ secrets.LEARNING_VM_HOST }}
      LEARNING_VM_USER: ${{ secrets.LEARNING_VM_USER }}
      LEARNING_VM_SSH_KEY: ${{ secrets.LEARNING_VM_SSH_KEY }}
    steps:
      - name: Skip deploy when SSH secrets are absent
        if: env.LEARNING_VM_HOST == '' || env.LEARNING_VM_USER == '' || env.LEARNING_VM_SSH_KEY == ''
        run: echo "Skipping deploy because learning-vm SSH secrets are not configured."
      - uses: appleboy/ssh-action@v1.2.0
        if: env.LEARNING_VM_HOST != '' && env.LEARNING_VM_USER != '' && env.LEARNING_VM_SSH_KEY != ''
        with:
          host: ${{ env.LEARNING_VM_HOST }}
          username: ${{ env.LEARNING_VM_USER }}
          key: ${{ env.LEARNING_VM_SSH_KEY }}
          script: |
            set -e
            cd /opt/odinbook/src/OdinBook-server
            git fetch origin main
            git reset --hard origin/main
            cd /opt/odinbook
            docker compose up -d --build server caddy
            docker compose exec -T server node -e "fetch('http://localhost:3000/health').then(r => { if (!r.ok) process.exit(1); })"
